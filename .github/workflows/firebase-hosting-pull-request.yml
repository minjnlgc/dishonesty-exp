# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on PR
on: pull_request
permissions:
  checks: write
  contents: read
  pull-requests: write
jobs:
  build_and_preview:
    if: ${{ github.event.pull_request.head.repo.full_name == github.repository && contains(toJson(github.event.head_commit.message), '#deploy') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: ./build_and_extract.sh
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_DISHONESTY_EXP_15A5B }}
          projectId: dishonesty-exp-15a5b
